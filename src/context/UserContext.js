//\\ بسم الله الرحمن الرحيم //\\
//myimports
import { createContext } from "react"; //imported
//myimports
const UserContext = createContext(); //initialized
export default UserContext; //Exported
// In your src folder, create a context folder,
// and inside it add a UserContext file.
// Now import createContext and initialize a
// UserContext. (Export it)
